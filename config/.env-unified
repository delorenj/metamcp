# MetaMCP Unified Environment Configuration
# This single file configures all deployment modes: production, development, test

# =============================================================================
# APPLICATION MODE CONFIGURATION
# =============================================================================
NODE_ENV=production
APP_MODE=production  # Options: production | development | test

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Application URLs (adjust for your deployment)
APP_URL=http://localhost:12008
NEXT_PUBLIC_APP_URL=${APP_URL}

# Port Configuration
FRONTEND_PORT=12008      # Next.js frontend port
BACKEND_PORT=12009       # Node.js backend port
POSTGRES_EXTERNAL_PORT=9433  # PostgreSQL external port

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL Connection
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=metamcp_user
POSTGRES_PASSWORD=m3t4mcp
POSTGRES_DB=metamcp_db

# Composed Database URL (used by applications)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Authentication Secret (CHANGE THIS IN PRODUCTION!)
BETTER_AUTH_SECRET=your-super-secret-key-change-this-in-production

# =============================================================================
# OIDC PROVIDER CONFIGURATION (Optional)
# =============================================================================
# Uncomment and configure these variables to enable OpenID Connect authentication
# OIDC_CLIENT_ID=your-oidc-client-id
# OIDC_CLIENT_SECRET=your-oidc-client-secret
# OIDC_DISCOVERY_URL=https://your-oidc-provider.com/.well-known/openid-configuration
# OIDC_AUTHORIZATION_URL=https://your-oidc-provider.com/auth

# Optional OIDC Configuration
# OIDC_PROVIDER_ID=oidc
# OIDC_SCOPES=openid email profile
# OIDC_PKCE=true

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# Docker networking fixes
TRANSFORM_LOCALHOST_TO_DOCKER_INTERNAL=true

# Volume mounting configuration
CONFIG_DIR=./config                    # Config directory to mount
SOURCE_MOUNT_TYPE=volume               # volume | bind (for development)
SOURCE_MOUNT_SOURCE=empty_volume       # empty_volume | . (for development)

# Container restart policy
RESTART_POLICY=unless-stopped          # unless-stopped | always | no

# Optional PostgreSQL configuration directory
POSTGRES_CONFIG_DIR=./config/postgres

# =============================================================================
# MODE-SPECIFIC OVERRIDES
# =============================================================================
# These variables can be overridden for different deployment modes

# Development Mode Overrides (set APP_MODE=development)
# SOURCE_MOUNT_TYPE=bind
# SOURCE_MOUNT_SOURCE=.
# RESTART_POLICY=no

# Test Mode Overrides (set APP_MODE=test)
# POSTGRES_DB=metamcp_test_db
# RESTART_POLICY=no

# Production Mode Overrides (set APP_MODE=production)
# [Uses defaults above]

# =============================================================================
# DEPLOYMENT EXAMPLES
# =============================================================================
# 
# 🚀 Production deployment:
#   docker-compose up -d
#
# 🔧 Development with hot reload:
#   APP_MODE=development SOURCE_MOUNT_TYPE=bind SOURCE_MOUNT_SOURCE=. docker-compose up
#
# 🧪 Test mode:
#   APP_MODE=test POSTGRES_DB=metamcp_test_db docker-compose up
#
# 🐳 Build and deploy:
#   docker-compose up --build -d
#
# =============================================================================